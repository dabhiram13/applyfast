# Paste this into Codex after setup

You are working inside the Applyfast repo. Read AGENTS.md and docs/*.md first.

Task:
1. Run npm install.
2. Run npm run build.
3. Fix only build/runtime errors caused by the Applyfast setup.
4. Do not add new product features.
5. Do not build Chrome extension, mass apply, inbox, or billing.
6. Report changed files and exact commands to test locally.

The target product is the visa-aware job decision engine, not a generic job board.
