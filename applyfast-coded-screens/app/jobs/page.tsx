import { AppShell } from "@/components/AppShell";
import { Badge, Button, Card, CompanyLogo, Score } from "@/components/UI";
import { matches } from "@/lib/mock";

export default function JobsPage() {
  const selected = matches[0];
  return <AppShell active="Job Matches"><div className="app-top"><div><h1 className="app-title">Job Matches</h1><p className="p">Personalized opportunities based on your profile and preferences.</p></div><Button variant="outline">Save Search</Button></div><div className="filter-bar" style={{ gridTemplateColumns:"repeat(5,1fr) auto auto" }}><select className="input"><option>All Roles</option></select><select className="input"><option>All Locations</option></select><select className="input"><option>H-1B</option></select><select className="input"><option>Remote / Hybrid</option></select><select className="input"><option>$120k+</option></select><Button variant="outline">More Filters 2</Button><Button variant="outline">Reset</Button></div><div className="sponsor-grid"><Card className="card-pad"><div className="list-title"><div className="pill-row"><Badge tone="green">Best Matches 126</Badge><Badge>Recent 32</Badge><Badge>Saved 18</Badge><Badge>Applied 9</Badge></div><select className="input" style={{ maxWidth:170 }}><option>Best Match</option></select></div>{matches.concat(matches.slice(0,3)).map((job,i)=><div className={`company-row ${i===0?"selected":""}`} key={i}><CompanyLogo color={job.color}>{job.logo}</CompanyLogo><div><strong>{job.role}</strong><p className="p">{job.company} · {job.location}</p><div className="pill-row"><Badge>{job.salary}</Badge><Badge tone="green">H-1B Sponsor</Badge></div></div><div><strong>Ghost rate</strong><p className="p">Low</p></div><div><strong>Reply</strong><p className="p">1–2 weeks</p></div><Score value={job.score}/><span>♡</span></div>)}</Card><Card className="card-pad"><div className="detail-head"><CompanyLogo color={selected.color}>{selected.logo}</CompanyLogo><div><h2 className="h3">{selected.role}</h2><p className="p">{selected.company} · {selected.location}</p><div className="pill-row"><Badge>{selected.salary}</Badge><Badge tone="green">H-1B Sponsor</Badge></div></div><Score value="96%"/></div><div className="pill-row" style={{ marginTop:18 }}><Button>Apply on Stripe</Button><Button variant="outline">Save</Button><Button variant="outline">•••</Button></div><div style={{ marginTop:24 }}><div className="pill-row"><Badge tone="green">Overview</Badge><Badge>Why it matches</Badge><Badge>Company</Badge></div><p className="p" style={{ marginTop:16 }}>Build reliable payment infrastructure used by millions of businesses worldwide.</p></div><Card className="card-pad" style={{ marginTop:18 }}><h3>Why this matches you</h3><p>✓ Backend engineering experience with distributed systems</p><p>✓ Experience with TypeScript, Python, and PostgreSQL</p><p>✓ Strong sponsor evidence and hybrid location fit</p></Card><h3 style={{ marginTop:18 }}>Visa support evidence</h3><div className="helper-card"><strong>High confidence</strong><p className="p">H-1B sponsorship offered for this role, strong sponsorship track record, and role-level evidence found.</p></div></Card></div></AppShell>;
}
